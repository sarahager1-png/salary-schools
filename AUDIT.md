# דוח ביקורת — מערכת שכר מורים (salary-schools)

## 1. שורה תחתונה

הארכיטקטורה הרעיונית של המערכת נכונה ואפילו חכמה: ההחלטה ש**הנתון המחייב מגיע מהמחשבון הרשמי** ולא מטבלה פנימית (`calcGross:96`) מנטרלת מראש את רוב הסיכון החישובי, ומודל הסטטוסים (`needsSim/needsApproval/isPending`) הוא הפשטה נקייה שמשמשת עקבית בכל המסכים. גם מנוע השעות של אופק חדש, תיעוד ה"לפני/אחרי" בשינויים ומסך הסימולטור עם ה-iframe הם עבודה טובה באמת.

**אבל המערכת במצבה הנוכחי לא ניתנת לשימוש בפועל.** אי אפשר להוסיף אף מורה דרך הממשק — שני נתיבי ההוספה שבורים, אחד בשקט ואחד ב-ReferenceError. הטופס המלא (`TeacherModal`, 419 שורות) בלתי ניתן להגעה, ואיתו כל עריכת תפקיד/שלב/קבוצת גיל. ארבע כתובות המחשבון הרשמי מצביעות לראוטים שלא קיימים באתר, כך שבורר המחשבונים דקורטיבי. ומעל הכול — אין אימות, אין בקאנד, ואין ייצוא: שלושת התפקידים לא רואים זה את עבודתו של זה, וכל התקציב חי ב-localStorage של דפדפן אחד בלי כפתור גיבוי.

החולשה המרכזית אינה החישוב אלא **שרשרת ההסתמכות**: הזרימה מנהלת→חשבת→שליח מתוכננת יפה בקוד אבל לא קיימת במציאות, והדרך היחידה שנתונים נכנסים למערכת היום היא ייבוא CSV — שהפרסר שלו נשבר על שם עם פסיק ועל קובץ שנשמר מאקסל.

---

## 2. מה יש — ועובד

**מנוע חישוב**
מודל שעות אופק חדש נכון (26/5/5 ביסודי, 23/4/9 בחט"ב, `LEVELS:58-62`); הפחתות גיל מיושמות נכון רעיונית — מורה בת 55 שמלמדת 23 שעות עדיין 100% משרה ולא נקנסת (`deriveHours:103-117`); הפרדה נקייה בין ברוטו לעובד לעלות מעסיק (`calcEmployer:141-147`); ימי הבראה מדורגים לפי ותק ולא מספר קבוע; גמול תפקיד עם רצפת מינימום.

**זרימה וסטטוסים**
שלושה predicates נגזרים משלושה שדות בלבד, בשימוש עקבי בכל התצוגות; שינוי בשדה שכר בסיסי מאפס אוטומטית סימולציה ואישור; `snapT/diffT/TeacherDiff` מציגים לשליח בעברית קריאה בדיוק מה השתנה; פתיחת חודש חדש יוצרת רשומה נפרדת ומאפסת שדות חודשיים.

**נתונים**
`load/loadObj` עטופים ב-try/catch; מיגרציה עובדת מהמבנה הישן השטוח למבנה החודשי; עזרי חודשים מטפלים נכון בגלישת שנה; `EMPTY_TEACHER` הוא מקור אמת יחיד לסכימה.

**סימולטור וייבוא**
מסך עבודה אמיתי לחשבת: iframe בצד אחד, רשימת ממתינים בצד השני, Enter שומר וקופץ למורה הבא עם פלאש ✓; `ImportModal` בזרימת 3 שלבים עם תצוגה מקדימה מלאה — אין ייבוא עיוור; תבנית CSV עם BOM שנפתחת נכון בעברית באקסל.

**דוחות ועיצוב**
שלושה דוחות מובנים (בית ספר, ממ"מ/העדרויות, רשת) עם כרטיסי סיכום ושורות סה"כ; `tabular-nums` על כל תא מספרי; טבלה רחבה עם כותרת ועמודת שם דביקות; `prefers-reduced-motion` מכובד; טוקני CSS מרוכזים ב-`:root`.

---

## 3. מה חסר — לפי חומרה

### 🔴 קריטי

**1. אי אפשר להוסיף מורה. בכלל.**
`startNew` (1597) יוצר רשומה עם `id: uid()` — כלומר id כבר truthy. `onSaveTeacher` (2338-2363) עושה `t.id ? teachers.map(...) : [...teachers, ...]` — ה-map עובר על מערך שאין בו את ה-id החדש ומחזיר עותק זהה. **הרשומה נעלמת בלי שגיאה ובלי הודעה**, ושורת העריכה נסגרת כאילו נשמר. הכפתור השני (`openAddTeacher(s.id)`, 2581) זורק `ReferenceError` — הפונקציה לא מוגדרת בשום מקום, ESLint מדווח עליה כ-`no-undef`. הנתיב היחיד שעובד הוא ייבוא CSV.
*תיקון: `teachers.some(x => x.id === t.id) ? map : append` — שורה אחת.*

**2. `TeacherModal` מת — ואיתו רבע מהמערכת.**
מוגדר ב-888, מרונדר ב-2604, אבל `setTeacherModal` נקרא רק עם `null` (2293, 2362). לכן `set('role')` (1167), `set('level')` (1038) ו-`set('ageGroup')` (1054) — שקיימים **רק** שם — לא נגישים. `ScopeChangeModal` ו-`FileAttachSection` מתים איתו. התוצאה החישובית: `calcRoleSupp` ו-`AGE_RED` רצים לנצח על ברירת המחדל, כלומר גמול תפקיד והפחתת שעות גיל אף פעם לא נכנסים לעלות המעסיק.

**3. אין אימות — הכניסה היא בורר תפקיד חופשי.**
`LoginScreen` (233-312) מחזיק `useState('coordinator')` ו-`schoolId`. אין סיסמה, אין זהות, אין קישור בין אדם לבית ספר. כל מי שפותח את הכתובת נכנס כשליח ומאשר תקציב שכר. כפתור "יציאה" מחזיר לבורר, ושם המנהלת בוחרת כל בית ספר ברשת.

**4. מנהלת יכולה להזין שכר רשמי ולעקוף את חשבת השכר.**
בשורת העריכה בטבלה: שדה `_officialGross` (1780) פתוח לכולם, בעוד `_officialGrossPre` (1781) מוגן ב-`!isPrincipal` — אותה אי-עקביות בדיוק חוזרת ב-1855/1856. `_officialGross` אינו ב-`BASE_FIELDS`, ולכן השמירה לא מאפסת דבר — המורה קופצת מ"נדרשת סימולציה" ל"מוכן לאישור" בלי שהורצה סימולציה.

**5. "אשר הכל" מאשר את כל מורי הרשת.**
`onApproveAll` (2379-2383) עושה `teachers.map` על **כל** מורי החודש — כל בתי הספר, כולל מי שלא שינה דבר וכולל מי שאין לו שכר רשמי — ומציב `_approved:true`. הכפתור מציג `readyToApprove.length` שמסונן ב-`needsApproval`, כך שהמספר על הכפתור לא מתאר מה הפעולה עושה. מורה בלי שכר רשמי נעלמת אחריו מכל רשימות העבודה.

**6. כתובות המחשבון שגויות — והחשבת לא תדע.**
הראוטים בבאנדל החי של `educalc.unq.co.il` הם שמיים: `OfekHadash`, `OfekNihul`, `OldWorld`, `OzLetmura`. בקוד: `{ofek:'#/Calculators/2', oz:'/3', mgmt:'/4', old:'/1'}` (2094-2099, ושוב ב-895-899) — **אף אחת מארבע הכתובות לא קיימת**. בורר המחשבונים דקורטיבי: החשבת חושבת שהיא במחשבון טרום-רפורמה ומזינה מספר שחושב במסך אחר. בנוסף אין `onLoad`/`onError`/`timeout` ואין "פתח בחלון חדש" — בדפדפן שחוסם עוגיות צד-שלישי (האתר מחזיר לולאת 302 בלי עוגיות) החשבת רואה אזור ריק בלי הסבר.

**7. אין מקור נתונים משותף, ואין גיבוי.**
`import { useState }` בלבד — אין `useEffect`, אין fetch, אין supabase, אין storage listener. הכול ב-localStorage מקומי, כך ש"ממתין לסימולציה" של מנהלת אחת לעולם לא יגיע לחשבת במחשב אחר — **זרימת האישורים היא הדמיה**. במקביל אין שום ייצוא JSON/גיבוי: שתי ה-`a.download` היחידות בקובץ הן תבנית CSV ריקה וקובץ מצורף בודד. ניקוי site data = אובדן כל תקציב השכר, בלי אזהרה ובלי שחזור. בשתי לשוניות באותו דפדפן — `persistMT` בונה `{...months, [mk]: ts}` מה-state המקומי ודורס חודשים שלמים שהלשונית השנייה עדכנה.

**8. חריגת מכסת אחסון תוך שנה — ובשקט.**
`openNewMonth` משכפל את **כל** רשומת המורה לכל חודש. מדידה: רשומה טיפוסית ≈809 תווים → 900 מורות × 12 חודשים ≈ 16.6MB ב-UTF-16, מעל פי שלושה מהמכסה. ו-`MONTHLY_RESET` מכיל `sickFiles:[]` אבל **לא** `_files` — כלומר כל קובץ base64 (עד 2MB) מועתק לכל חודש חדש. 12 עותקים של אותו PDF בשנה. ו-`save` (196) הוא `localStorage.setItem` **חשוף בלי try/catch** בזמן ש-`load`/`loadObj` דווקא עטופים — `QuotaExceededError` ייזרק אחרי ש-`setMonths` כבר נכנס לתור React, כך שהמסך יראה "נשמר" והדיסק לא יתעדכן.

**9. אין שום ייצוא נתונים — לא CSV ולא אקסל.**
grep על blob/csv/xlsx/clipboard מחזיר רק את `downloadTemplate` (689-705) — קובץ קבוע עם שלוש שורות דוגמה מומצאות. אין המרה של `teachers`/`rows` ל-CSV, אין ספריית xlsx, אין העתקה ללוח. מסלולי הפלט היחידים הם `window.print()` ו-mailto. חשבת שכר לא יכולה להוציא קובץ לטעינה למערכת השכר — וזו כל מטרת המערכת.

---

### 🟠 גבוה

**10. `BASE_FIELDS` / `TRACKED` / `FIELD_LBL` — שלוש רשימות ידניות שיצאו מסנכרון.**
`role` **חסר מ-`BASE_FIELDS`** למרות ש-`calcRoleSupp` משפיע ישירות על `calcGross` — מעבר ל"סגן/ית מנהל" הוא 20% מהבסיס והוא לא מפיל את השכר הרשמי המאושר. `level` ו-`ageGroup` באותה סירה (משנים שעות דרך `deriveHours`). הפוך: `childrenUnder18` ב-`BASE_FIELDS` ולא ב-`TRACKED` — מפיל אישור אבל לא מופיע ב-diff, השליח רואה "ממתין" בלי לדעת ממה. ו-`isTemp` ב-`TRACKED` וחסר מ-`FIELD_LBL` → שורת diff עם תווית ריקה.

**11. שינוי אחרי אישור לא מפיל את האישור.** נגזרת ישירה של 10: `role`, `level`, `ageGroup`, `isTemp`, `startDate`, `endDate` ו-`_officialGross` נשמרים בשקט — `_approved` נשאר true, `_changedAt` נשאר null, ואין snapshot.

**12. `currentScope` מתעלמת מהחודש ומהאישור.**
`[...t.scopeChanges].sort(by date).at(-1)` (89-94) — בלי פרמטר חודש, בלי לבדוק שהתאריך הגיע, בלי לבדוק `c._approved`. שינוי שנרשם באוגוסט עם תאריך 01/09 משפיע מיד על אוגוסט. והתנאי `c._approved` (322) **מת**: שום קוד לא כותב `_approved` על אובייקט scopeChange. זה נכנס גם ל-`calcExtras`, ולכן משנה הבראה וביגוד בעלות המעסיק גם למורות עם שכר רשמי. המערכת חודש-חודש, ולמנוע החישוב אין פרמטר חודש בכלל.

**13. `scope` מול `scopePct` — שינוי משרה לטרום-רפורמה לא משפיע על כלום.**
`calcGross:99` ו-`calcExtras:133` קוראות `t.scope` לטרום. עורך הטבלה — הנתיב היחיד לעריכה — כותב **אך ורק** `scopePct` (1750). כלומר מורה בטרום-רפורמה שהורדה ל-50% משרה תיספר בתקציב עם הבראה וביגוד של משרה מלאה. רק נתיב הייבוא מסנכרן את שני השדות (648-649).

**14. כל 14 טבלאות השכר הן אינטרפולציה לינארית מומצאת.**
`buildBase` (15-19) מייצרת 30 ערכים בקו ישר — אופק ד1 = מדרגה של 96.7 ₪ בדיוק לכל שנת ותק. אין מקור ואין שנה. הארכיטקטורה מגינה: `TeacherModal` מציג "נדרשת סימולציה" ולא הערכה, ו-`SchoolView`/`ReportView` מסננים `tsOfficial`. **אבל הערך דולף**: `buildEmailBody` (533-534) ודשבורד השליח (2547) מסכמים על כל המורות ללא סינון, ותור האישורים (410) מציג עלות מעסיק למורה שאין לה שכר רשמי.

**15. הדוח שיוצא במייל מערבב הערכות מומצאות עם נתונים רשמיים — בלי סימון.** ובתוך `SchoolReport` עצמו: עמודת "ברוטו למעסיק" לכל שורה מציגה `emp.total` המבוסס על הערכה (1411), בעוד ה-tfoot מסכם רק מורות רשמיות (1418-1424) — **הטור לא מסתכם לסיכום שלו, באותו דף מודפס**.

**16. `mailto` לא באמת שולח, ונחתך.** `window.open(mailto:...)` בלבד — אין לוג, אין קובץ מצורף, אין אישור. כל תו עברי מתנפח ל-9 תווים ב-`encodeURIComponent`; 25-30 מורות ≈ 9,000 תווים, מעל מגבלת ~2,000 של כרום/אאוטלוק. המייל יישלח קטוע באמצע רשימת המורות. אם `principalEmail` ריק — נפתח חלון בלי נמען, והכפתור לא חוסם.

**17. חודש חדש נפתח עם שכר החודש הקודם ובלי סטטוס.** `MONTHLY_RESET` לא מאפס `_officialGross`/`_officialGrossPre`, ומכיוון ש-`needsSim`/`needsApproval` דורשים `_changedAt` שזה עתה אופס — המורים לא ברשימת החשבת, לא ברשימת האישורים ולא מאושרים. **הם נעלמים מכל רשימות העבודה** בזמן שהטבלה מציגה את שכר החודש הקודם כשכר של החודש הזה.

**18. אין audit trail, אין ביטול אישור, אין נעילת חודש.** `_approvedBy` לא קיים בקובץ; `_approvedAt` נכתב ואף פעם לא מוצג. האישור **מוחק ראיות**: `_snapshot:null` ו-`_changedAt:null`. אין `onRevoke`, אין מחרוזת `locked`, ובורר החודש בהדר פתוח לכל תפקיד — אפשר לפתוח חודש ששולם ולערוך מעליו.

**19. הדפסה נשברת — וזה מסלול הפלט היחיד.** שני הדוחות עטופים ב-`position:'fixed', inset:0, overflowY:'auto'` (1342, 1489), ובלוק ה-print ב-`index.css:499-504` מכיל שלושה כללים בלבד — אין `position:static`, אין `overflow:visible`, אין `@page`. בכרום תוכן שגולל בתוך fixed מודפס רק לפי מה שנראה בחלון: **טבלה ארוכה נחתכת אחרי עמוד אחד**. בנוסף header של `AbsenceReport` בלי `no-print` (הכפתורים יודפסו), ואף כלל לא מסתיר את הגיליון שמאחורי המודל.

**20. `monthlyExtras` והעדרויות לא נכנסים לאף סיכום עלות.** `calcEmployer` בנוי מ-`calcGross + calcExtras` בלבד. התוספות החודשיות מסוכמות רק ב-`totMonthly` וב-`AbsenceReport` — בדיוק המסכים שלא הולכים לתקציב. "ברוטו למעסיק" ו"עלות שנתית" נמוכים מהעלות בפועל, בלי הערה.

**21. פרסור CSV נאיבי + קידוד קשיח + אין זיהוי כפילויות.** `split(',')` ואז הסרת מרכאות (618) — `"כהן, שרה"` מייצר 4 עמודות במקום 3 וכל השדות זזים, בלי אזהרה. `readAsText(f,'utf-8')` קשיח (717) — מנהלת ששומרת מאקסל ב"CSV מופרד בפסיקים" מקבלת ANSI/1255, השמות ייובאו כג'יבריש והבדיקה היחידה היא `parsed.length===0`. זיהוי הפורמט תלוי ב-`cols[2].includes('@')` — טלפון במקום מייל מזיז 11 עמודות ל-9. ו-`onImportTeachers` עושה append עיוור: ייבוא חוזר של אותו קובץ **מכפיל את כל בית הספר**, למרות ש-`tzId` נאסף וזמין.

**22. אין ולידציה בטפסים.** ת"ז ללא ספרת ביקורת, מייל ללא `type="email"`, וכל שדות ה-number (`scopePct`, `seniority`, `frontalHours`, `absenceDays`) בלי `min`/`max` — `-50` או `9999` נכנסים ישירות ל-`calcEmployer`. הממשק אפילו כותב "תאריך סיום — חובה" (990) ולא אוכף (1290-1292).

**23. נגישות מקלדת חסומה בזרימות מרכזיות.** `grep tabIndex` → 0 מופעים ב-2,607 שורות. כרטיס בית ספר (2554), כרטיס מורה בסימולטור (2214) והלוגו (2394) הם divs עם onClick. הכי חמור: שדה השכר הרשמי מרונדר רק בתוך `{isActive && ...}` ו-`isActive` נקבע אך ורק מה-onClick — **משתמש מקלדת לא יכול לפתוח את השדה כלל**. חמישה מודלים פעילים ללא `role="dialog"`, ללא מלכודת פוקוס וללא Escape; `grep role="` → 0.

**24. חשבת השכר לא יכולה להזין את סימולציית "טרום".** `onSaveGross` מקבל ערך יחיד, `SimulatorView` מציג input אחד, ו-`_officialGrossPre` קיים רק ב-`TeacherModal` המת. הבלוק שמחשב "תוספת בית חב"ד = אופק − טרום" מותנה בשני השדות — כלומר **התוספת לעולם לא מחושבת בזרימה האמיתית**.

**25. אפס בדיקות, אפס TypeScript, אפס ErrorBoundary — ו-11 שגיאות ESLint פתוחות.** `main.jsx` הוא StrictMode+App בלבד; שגיאת רנדר = מסך לבן במערכת שכל הנתונים שלה ב-localStorage. בדיקת smoke אחת של "הוסף מורה → היא מופיעה בטבלה" הייתה תופסת את שני הבאגים הקריטיים הראשונים.

**26. אין ייצוג של "עוז לתמורה" במודל.** הבורר מציע רק `ofek`/`pre`, אין ערך `oz` ואין טבלה — בזמן ש-`SimulatorView` באמת מציעה לחשבת את מחשבון עוז. בנוסף `LEVELS.high` זהה לחלוטין ל-`middle`, כלומר חטיבה עליונה מקבלת מבנה שעות של חטיבת ביניים, ו-`deriveHours` מחזירה `null` לכל מי שאינו אופק.

**27. `childrenUnder18` נאסף, מוצג כזכאות — ולא נכנס לשום חישוב.** הממשק אומר מפורשות "✓ זכאית לתוספת אם" ואז מחשב בדיוק כאילו אין ילדים. אין ולו הופעה אחת של השדה בין שורות 95-147.

**28. "עלות שנתית" = חודש אחד × 12.** בשני מקומות (1991, 1695). מתעלם מהחודשים שכבר נצברו ב-`months`, מ-`endDate` של שיבוצים זמניים ומהעדרויות. (ההבראה דווקא נכונה — `calcExtras` כבר מפזר אותה ÷12.)

**29. מנהלת ללא בית ספר תקף מקבלת את מסך השליח המלא** — כולל מחיקת בתי ספר, שמוחקת גם את כל מוריהם.

---

### 🟡 בינוני

- **`calcNet = ברוטו × 0.735`** (102) — ניכוי שטוח בלי מדרגות מס ובלי נקודות זיכוי. נקרא פעם אחת בלבד ומסומן "נטו משוער", כך שההשפעה התקציבית אפס — אבל זה מספר חסר-בסיס שיושב ליד הנתון הרשמי.
- **פירוק ה-30% שמוצג כמקור סמכות** (1469-1470) — "7.5% + 15% + 7.5%" מסתכם ל-30 אבל לא תואם את הרכיבים בפועל, ואין התייחסות למס שכר של מלכ"ר. המספר קשיח, בלי מקור ובלי שנה.
- **30% מחושבים גם על הבראה וביגוד** (143-145) — רכיבים שאינם פנסיוניים. ~324 ₪ מיותרים לחודש למורה בותק 25, שמנפחים כל סיכום.
- **`HAVRAAH_DAY=421 // 2024` ו-`BIGUUD_ANNUAL=2028`** — ערכים בני שנתיים כבסיס לתקציב חי, בלי מקום לעדכון שנה.
- **ותק נחתך בשקט ב-29** בעוד הסליידר מאפשר 40, ו-`havraahDays` דווקא לא חותך — שני חישובים מפרשים את אותו שדה אחרת.
- **מנהלת בית ספר מקבלת גמול תפקיד 0** בהערכה (`pct:0`), ו-`vp=20%` קבוע.
- **`parseGrade` מחזירה `'PHD'`** שלא קיים ב-`PRE_SALARY` → נפילה שקטה לטבלת BA, וגם `DEGREE_LABELS` יציג `PHD` גולמי.
- **`parseRole`**: `'principal'` לא ברשימת ה-known ולכן לא ניתן לייבוא; כל "יועצ..." מוחזר כ-`counselor` (12%) לפני שנבדק `counselor2` (18%) — **6% פער על כל יועצת קבועה**.
- **`level`/`ageGroup`/`childrenUnder18` חסרים בתבנית הייבוא ובפרסור**, ו-`frontalHours: Math.round(26*scope/100)` מקודד 26 קשיח בשני ענפי הפרסור — כל מורה מיובאת מקבלת נתוני יסודי, גם בחטיבה.
- **ענף הטקסט החופשי (וואטסאפ)** קורא 6 שדות בלבד; הרגקס `/^([^\n:]{2,20})$/m` יתפוס "בוקר טוב" כשם מורה.
- **`--text3: #8878AA`** נותן 3.95:1 מול לבן, ובפועל ~3.87:1 על רקע `--bg` בכותרות הטבלה בגופן 11px — מתחת ל-WCAG AA. טוקן אחד, תיקון אחד.
- **ה-toggle ללא סימון פוקוס כלשהו** — `opacity:0;width:0;height:0` מסתיר את הצ'קבוקס ואין כלל `:focus` חלופי (לשדות הקלט כן נבנתה טבעת).
- **תווית החודש ריקה בדוח ההעדרויות של השליח** — `SchoolView` בנתיב השליח לא מקבל `activeMonth`/`fmtMonthFn`.
- **הדוח במייל תמיד נושא את החודש הקלנדרי הנוכחי** — `new Date()` ב-`buildEmailBody`, בזמן שיש בורר חודשים מלא.
- **אין השוואה בין חודשים** — `months` קיים ואף פעם לא מועבר ל-`ReportView`.
- **`officialCount` מחושב (1991) ו-0 שימושים** — דוח הרשת מציג "12 מורות · 40,000 ₪" גם כשרק ל-4 יש שכר רשמי.
- **אין פירוק רכיבים סוציאליים** לפקודת יומן, ואין דוח ברמת מורה בודדת.
- **עמודת "סוציאלי" בגיליון (1925-1927) מציגה בפועל `emp.extras.total`** — ביגוד+הבראה, לא את ה-30%.
- **"או גררי לכאן"** — גרירה לא ממומשת; גרירת קובץ תנווט אל הקובץ ותאבד את המודל.
- **אין בדיקת שפיות על השכר שנשמר** — 94,000 במקום 9,400 נכנס ישר לתקציב הרשת, למרות ש-`est = calcGross(t)` מחושב שורה אחת מעל.
- **`onImportTeachers` ללא ולידציה**: `Number(seniority)||0`, `Number(scopePct)||100`, `tzId||''` — NaN הופך ל-0 ולריק הופך ל-100, בשקט.

---

### 🟢 נמוך

`uid()` לא קריפטוגרפי (מרחב 2.8×10¹², סיכון מעשי אפסי) · `_officialGross` חסר ב-`EMPTY_TEACHER` (אין `hasOwnProperty`/`in` בקובץ, אפס השפעה התנהגותית) · `colSpan={15}` בטבלה בת 21 עמודות + שימוש ב-`var(--apple-text3)` שאינו מוגדר · `loadObj(LS_MONTHS)` נקרא פעמיים באתחול · חיפוש ללא trim וללא case-insensitive · מד ההתקדמות מציג "0/42 הושלמו" בזמן שכתוב "כל המורים הוזנו" · `TeacherModal` חסר את מחשבון עוז · `window.confirm('למחוק?')` בלי שם המורה (בעוד מחיקת בית ספר כן מציגה שם) · אחוזי גמולי התפקיד ללא מקור ובלי תיעוד סדר החישוב מול היקף המשרה · ערכי `inputs` בסימולטור נדיפים — רענון מוחק סשן הזנה שלם · אין מיון/סינון בדוחות המודפסים.

---

## 4. הצעות ייעול — לפי יחס תועלת/מאמץ

### תשואה מיידית (impact גבוה / effort קטן)

| # | הצעה | פירוט |
|---|---|---|
| 1 | **תיקון זיהוי מורה חדשה** | `teachers.some(x => x.id === t.id) ? map : append` (2358), ו-`openAddTeacher` (2581) → `setTeacherModal({...EMPTY_TEACHER, schoolId: s.id})`. שתי שורות שמחזירות לחיים את זרימת ההוספה **ואת 419 שורות `TeacherModal`** עם role/level/ageGroup/שינויי משרה/קבצים. |
| 2 | **כפתור גיבוי מלא** | `JSON.stringify({v:1, schools, months})` → Blob → download, וקלט קובץ לשחזור. דפוס ה-Blob כבר קיים ב-`downloadTemplate`. 15 שורות מול הסיכון הכי גדול במערכת. כדאי להפעיל ייצוא אוטומטי ב-`openNewMonth`. |
| 3 | **`save` ב-try/catch + שמירה לפני setState** | alert אמיתי במקום אובדן שקט, והיפוך הסדר ב-`persistS/persistMT` כך שהמסך לא יציג נתונים שלא נכתבו. |
| 4 | **`_files:[]` ל-`MONTHLY_RESET`** | שורה אחת שעוצרת שכפול base64 של כל מסמכי המורים לכל חודש. |
| 5 | **תיקון כתובות המחשבון + fallback** | מפה שמית (`OfekHadash`/`OzLetmura`/`OfekNihul`/`OldWorld`) בשני המקומות, ו-`onLoad` עם timeout של 8 שניות שמציג "הדפדפן חוסם עוגיות" + כפתור `target="_blank"`. |
| 6 | **בחירת מחשבון אוטומטית לפי המורה** | ב-`setActiveId` לקבוע גם `setCalc` לפי `t.reform`. ההגנה האמיתית מפני הזנה מהמחשבון הלא-נכון. |
| 7 | **`toCSV(rows, headers, filename)` גנרית** | לחלץ מ-`downloadTemplate` (BOM+Blob+download כבר שם) ולחבר לשלושת הדוחות. עבודה קטנה מול הפער הגדול ביותר. |
| 8 | **תיקון בלוק ההדפסה** | `@page{size:A4 landscape;margin:10mm}` + ביטול `position:fixed`/`overflow` למודל + `thead{display:table-header-group}` + `tr{break-inside:avoid}` + `print-color-adjust:exact`. מתקן חמש בעיות במכה. |
| 9 | **מקור אמת אחד לשדות** | `FIELDS = { role: {label, affectsSalary, tracked}, ... }` ולגזור ממנו את שלוש הרשימות. מבטל את באג `role`/`level`/`ageGroup` **ומונע את חזרתו** בכל שדה עתידי. |
| 10 | **איחוד `scope` ו-`scopePct`** | להשאיר `scopePct` בלבד — מוחק ארבע התפצלויות ternary זהות ומבטל את באג טרום-הרפורמה. |
| 11 | **זיהוי קידוד אוטומטי בייבוא** | `readAsArrayBuffer` → `TextDecoder('utf-8',{fatal:true})` → fallback ל-`windows-1255`. פותר את "אקסל שמר ANSI" שיפגע כמעט בכל מנהלת. |
| 12 | **שדה "סימולציית טרום" ב-`SimulatorView`** | שני שדות למורת אופק ו-`onSaveGross({gross, grossPre})`. בלי זה תוספת בית חב"ד לא מחושבת לעולם. |
| 13 | **`--text3` → ~`#6F5D93`** | 5.2:1, שומר על האופי הסגול-אפור, מתקן 21 כותרות טבלה + כל התוויות. טוקן אחד. |
| 14 | **שלושת ה-divs הלחיצים → `<button>`** | פותח את כל מסלול חשבת השכר ואת הכניסה לבית ספר למקלדת. + Escape בכל מודל, Enter/Escape בשורת העריכה. |
| 15 | **ESLint בשער** | husky+lint-staged או step ב-CI. רבע שעה שמונעת בדיוק את הבאג שהפיל פיצ'ר. |
| 16 | **`vitest` + 15-20 מקרי ייחוס** | הפונקציות טהורות וללא React — ניתנות לבדיקה היום. מקרי הייחוס יורצו פעם אחת ב-educalc ויוזנו כתשובה צפויה. הופך "כמה המנוע מדויק" ממחלוקת למספר מדיד. |
| 17 | **בדיקת שפיות + de-dup בייבוא** | סטייה מעל 30% מהאומדן → confirm; והצלבת `tzId` בתצוגה המקדימה עם "חדש/עדכון/כפילות". |
| 18 | **`officialCount` שכבר מחושב** | להציג "4 / 12" בצבע אזהרה ב-`ReportView`. שורה אחת שהופכת את הדוח לכן. |
| 19 | **`monthlyExtras` לתוך `calcEmployer`** | עלות אמיתית שלא מגיעה לתקציב. |
| 20 | **`crypto.randomUUID()` עם fallback** | שורה אחת. |

### תשואה גבוהה / מאמץ בינוני

- **פרמטר חודש לכל המנוע** — `(t, monthKey)`, ו-`currentScope` תבחר את השינוי האחרון שתאריכו ≤ סוף החודש **ושהוא מאושר**. פותר בבת אחת את השינוי העתידי, את הלא-מאושר, ואת היחסיות באמצע חודש.
- **`calcPayslip(t, monthKey)` אחת** במקום ארבע קריאות — מחזירה `{base, role, gross, biguud, havraah, social, employerTotal, warnings}` עטופה ב-`useMemo`. שדה `warnings` יחזיר בשקיפות "ותק נחתך ל-29", "תואר לא מוכר — חושב כ-BA", "אומדן בלבד".
- **`status(t)` אחת** במקום שלושה predicates — `'new'|'needs_sim'|'needs_approval'|'approved'|'locked'` שגוזרת תווית, צבע ופעולות מותרות. מבטלת את המצב ה"יתום" שנוצר בפתיחת חודש.
- **אירועים במקום דגלים** — `_events: [{type, by, at, from, to}]` פותר audit trail, ביטול אישור והיסטוריה ביחד, ומייתר את מחיקת הראיות באישור.
- **`can(user, action)`** — מפת הרשאות אחת במקום `isCoord/isClerk/isPrincipal` מפוזרים ו-props שמועברים כ-`null` כמנגנון הרשאה. מונעת בדיוק את הפער שבו `_officialGrossPre` מוגן ו-`_officialGross` לא.
- **PapaParse** במקום `split(',')`, ומערך `errors` שיוצג בתצוגה המקדימה כ"3 שורות נפסלו".
- **`useMemo` + `React.memo` על שורת הטבלה** — `calcEmployer` רץ ארבע פעמים על אותה רשימה ועוד פעם לכל שורה. אין ולו `useMemo` אחד בכל הקובץ.
- **הוצאת המנוע ל-`src/salary/engine.js` + `tables.2026.json`** עם `year` ו-`source` לכל טבלה, והצגת השנה בממשק. מונע את "2024 בהערה ו-2026 במציאות".
- **פיצול `App.jsx`** (2,607 שורות, 34 פונקציות, 563 אובייקטי style inline) ל-`lib/` + `components/`. הפיצול הטבעי כבר כתוב בהערות הבאנר.
- **zod על שכבת הקלט** — סכמה אחת שמשרתת גם את הטופס וגם את `parseTeachers`.

### מאמץ גדול — הפתרון האמיתי

- **מעבר ל-Supabase עם RLS לפי `schoolId`.** זה הצעד היחיד שסוגר בבת אחת: אימות אמיתי במקום בורר תפקיד, הרשאה שנאכפת בשרת ולא ב-JSX, עבודה מקבילה של שלושה תפקידים (שהיום פשוט לא קיימת), גיבוי, מכסת אחסון, קבצים דרך Storage, ו-audit trail. טבלאות: `schools`, `teachers`, `teacher_months`, `approvals`. RLS: מנהלת רואה רק את בית ספרה, חשבת כותבת רק `official_gross`, שליח בלבד כותב `approved`.
- **נורמליזציה** — רישום מורות אחד + דלתא חודשית במקום עותק מלא. מוריד את הצריכה פי 5-8 ומתקן בדרך את המורים היתומים. כדאי לעשות **יחד** עם המעבר ל-Supabase, לא לפניו.

---

## 5. מה הייתי עושה קודם

**צעד 1 — להחזיר את המערכת למצב שאפשר להשתמש בה (יום עבודה אחד).**
תיקון `persistT` (שורה אחת), החלפת `openAddTeacher` ב-`setTeacherModal`, `_files:[]` ל-`MONTHLY_RESET`, `save` ב-try/catch, וניקוי 11 שגיאות ה-ESLint. אחרי הצעד הזה אפשר להוסיף מורה, הטופס המלא חוזר לחיים, וההזנה מפסיקה להיכשל בשקט. **בלי זה כל שאר העבודה תיאורטית.**

**צעד 2 — כפתור גיבוי + ייצוא CSV (חצי יום).**
`toCSV` גנרית מחוברת לשלושת הדוחות, וכפתור "גיבוי מלא" שמייצא ומייבא JSON. זה מוריד את הסיכון הקיומי (אובדן כל התקציב בניקוי דפדפן) ופותר את הפער שבגללו חשבת השכר לא יכולה בכלל להשתמש בפלט. שני אלה יישארו רלוונטיים גם אחרי מעבר לבקאנד.

**צעד 3 — לתקן את הסימולטור (חצי יום).**
כתובות שמיות במקום מספריות, בחירת מחשבון אוטומטית לפי `t.reform`, `onLoad`+timeout עם "פתח בחלון חדש", ושדה "סימולציית טרום". זהו המסך שבו נוצר **הנתון היחיד שמחייב במערכת** — הוא לא יכול להיות דקורטיבי.

**צעד 4 — לסגור את שרשרת האמון בשכר (יום-יומיים).**
מקור אמת אחד לשדות (`FIELDS` שממנו נגזרים `TRACKED`/`BASE_FIELDS`/`FIELD_LBL`) — מתקן את `role`/`level`/`ageGroup` שלא מפילים אישור; פילטר על `onApproveAll`; חסימת `_officialGross` למנהלת; ואיחוד `scope`/`scopePct`. במקביל להוסיף סימון "רשמי / אומדן" בכל מקום שמסכם — מייל, דשבורד שליח, תור אישורים — כדי שהאינטרפולציה המומצאת תפסיק לדלוף כאילו היא נתון.

**צעד 5 — Supabase.**
רק אחרי שהצעדים 1-4 הפכו את הלוגיקה לנכונה. אין טעם להעביר לבקאנד מודל שבו `role` לא מפיל אישור ושינוי משרה עתידי משפיע על העבר — זה רק יקבע את הבאגים בסכימה. אבל אחרי שהמודל נקי, זה הצעד שהופך את זרימת האישורים מהדמיה למערכת אמיתית.

**הערה אחת בכנות:** ההחלטה הארכיטקטונית שלך — שהמספר המחייב מגיע מהמחשבון הרשמי — היא מה שמציל את המערכת הזאת. כל הליקויים החישוביים בדוח (טבלאות מומצאות, `calcNet`, גמול מנהלת, חיתוך ותק) הם ליקויים ב**אומדן**, לא בשכר ששולם. שמרי על העיקרון הזה גם כשתשפרי את הטבלאות — ובמקביל אטמי את שלוש הנקודות שבהן האומדן דולף החוצה כאילו הוא נתון רשמי.
