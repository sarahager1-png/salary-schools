/*
  `scope_pct_pre` — עמודה מתה, מסומנת ככזו.

  "יש פער בין עולם ישן לחדש על האחוז הנגזר מאופק" (שרה, 8.9): שתי
  הסימולציות רצות על **אותו** אחוז — זה שנקבע לפי אופק — והפער ביניהן
  הוא תוספת בית חב"ד. אין אחוז שני.

  זה מבטל את ההנחה של `20260901140000_scope_per_track`, שלפיה למורת
  אופק יש אחוז אחר בעולם הישן. העמודה נוצרה שם, מולאה ב-27 שורות
  (ב-13 מהן ערך שונה מ-`scope_pct`, בעיקר 90 מול 86 אצל מורות אופק),
  ומעולם לא נקראה בקוד — רק במיפוי השדות של store.js.

  העמודה נשארת בינתיים עם הנתונים שבה, מסומנת ולא בשימוש. גיבוי מלא
  ב-`backup-scope_pct_pre.json` בשורש הפרויקט. מחיקה בפועל — רק בהוראה
  מפורשת של שרה.

  בפועל הבסיס ממילא אינו מחושב מאחוז: חשבת השכר מזינה ברוטו ותוספת
  בית חב"ד, והבסיס הוא ההפרש. ראו `payBreakdown` ב-src/lib/employer.js.
*/
comment on column public.teacher_months.scope_pct_pre is
  'לא בשימוש (8.9.2026). שתי הסימולציות רצות על scope_pct, והפער ביניהן הוא תוספת בית חב"ד — אין אחוז נפרד לעולם הישן. הנתונים נשמרו; גיבוי ב-backup-scope_pct_pre.json.';

comment on column public.teacher_months.scope_pre_set_at is
  'לא בשימוש (8.9.2026) — ראו scope_pct_pre.';

notify pgrst, 'reload schema';
